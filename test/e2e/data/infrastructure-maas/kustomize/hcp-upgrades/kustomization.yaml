apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# No CNI CRS: HCP must use Cilium (Calico breaks the LXD bridge on the PXE interface), applied
# directly to the workload cluster by the hcp upgrade spec's control-plane-initialized waiter.
resources:
  - cluster-template.yaml
