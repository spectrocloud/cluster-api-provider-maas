---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: maasmachinetemplates.infrastructure.cluster.x-k8s.io
spec:
  group: infrastructure.cluster.x-k8s.io
  names:
    categories:
    - cluster-api
    kind: MaasMachineTemplate
    listKind: MaasMachineTemplateList
    plural: maasmachinetemplates
    singular: maasmachinetemplate
  scope: Namespaced
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: MaasMachineTemplate is the Schema for the maasmachinetemplates
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: MaasMachineTemplateSpec defines the desired state of MaasMachineTemplate
            properties:
              template:
                description: MaasMachineTemplateResource describes the data needed
                  to create a MaasMachine from a template
                properties:
                  spec:
                    description: Spec is the specification of the desired behavior
                      of the machine.
                    properties:
                      failureDomain:
                        description: |-
                          FailureDomain is the failure domain the machine will be created in.
                          Must match a key in the FailureDomains map stored on the cluster object.
                        type: string
                      image:
                        description: Image will be the MaaS image id
                        minLength: 1
                        type: string
                      lxdConfig:
                        description: |-
                          LXDConfig provides configuration for LXD VM provisioning
                          Only used when ProvisioningMode is "lxd"
                        properties:
                          hostSelection:
                            description: HostSelection defines how to select LXD hosts
                            properties:
                              availabilityZones:
                                description: AvailabilityZones lists preferred availability
                                  zones for host selection
                                items:
                                  type: string
                                type: array
                              preferredHosts:
                                description: PreferredHosts lists preferred LXD hosts
                                  by system ID
                                items:
                                  type: string
                                type: array
                              resourcePools:
                                description: ResourcePools lists preferred resource
                                  pools for host selection
                                items:
                                  type: string
                                type: array
                              tags:
                                description: Tags for host selection
                                items:
                                  type: string
                                type: array
                            type: object
                          networkConfig:
                            description: NetworkConfig defines network configuration
                              for the LXD VM
                            properties:
                              bridge:
                                description: Bridge specifies which bridge to connect
                                  the VM to
                                type: string
                              macAddress:
                                description: MacAddress allows specifying a custom
                                  MAC address
                                type: string
                              staticIPConfig:
                                description: StaticIPConfig defines static IP configuration
                                  for the LXD VM
                                properties:
                                  dnsServers:
                                    description: DNSServers is a list of DNS server
                                      IP addresses
                                    items:
                                      type: string
                                    type: array
                                  gateway:
                                    description: Gateway is the default gateway for
                                      the static IP configuration
                                    pattern: ^((25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)\.){3}(25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)$|^([0-9a-fA-F]{1,4}:){7}[0-9a-fA-F]{1,4}$
                                    type: string
                                  interface:
                                    description: |-
                                      Interface specifies the network interface to configure with static IP
                                      If not specified, the default interface will be used
                                    type: string
                                  ipAddress:
                                    description: IPAddress is the static IP address
                                      to assign to the VM
                                    pattern: ^((25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)\.){3}(25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)$|^([0-9a-fA-F]{1,4}:){7}[0-9a-fA-F]{1,4}$
                                    type: string
                                  subnet:
                                    description: Subnet defines the subnet mask or
                                      CIDR notation for the static IP
                                    pattern: ^((25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)\.){3}(25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)|\/([0-9]|[1-2][0-9]|3[0-2])$
                                    type: string
                                required:
                                - gateway
                                - ipAddress
                                - subnet
                                type: object
                            type: object
                          resourceAllocation:
                            description: ResourceAllocation defines resources for
                              the LXD VM
                            properties:
                              cpu:
                                description: CPU allocation in cores
                                minimum: 1
                                type: integer
                              disk:
                                description: Disk size in GB
                                minimum: 1
                                type: integer
                              memory:
                                description: Memory allocation in MB
                                minimum: 512
                                type: integer
                            type: object
                          storageConfig:
                            description: StorageConfig defines storage configuration
                              for the LXD VM
                            properties:
                              storagePool:
                                description: StoragePool specifies which LXD storage
                                  pool to use
                                type: string
                            type: object
                        type: object
                      minCPU:
                        description: MinCPU minimum number of CPUs
                        minimum: 0
                        type: integer
                      minMemory:
                        description: MinMemoryInMB minimum memory in MB
                        minimum: 0
                        type: integer
                      providerID:
                        description: ProviderID will be the name in ProviderID format
                          (maas://<zone>/system_id)
                        type: string
                      provisioningMode:
                        default: baremetal
                        description: |-
                          ProvisioningMode defines how this machine should be provisioned
                          Defaults to "baremetal" for backward compatibility
                        enum:
                        - baremetal
                        - lxd
                        type: string
                      resourcePool:
                        description: ResourcePool will be the MAAS Machine resourcepool
                        type: string
                      systemID:
                        description: SystemID will be the MaaS machine ID
                        type: string
                      tags:
                        description: Tags for placement
                        items:
                          type: string
                        type: array
                    required:
                    - image
                    - minCPU
                    - minMemory
                    type: object
                required:
                - spec
                type: object
            required:
            - template
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
